execution:
  command_executor_factory: com.non.existing.Clazz

filters:
  clusters:
    include:
    exclude:

  modules:
    include:
    exclude:

  tags:
    include:
    exclude:

commands:

scripts:
